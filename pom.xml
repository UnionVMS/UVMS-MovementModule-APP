<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>fish.focus.uvms.maven</groupId>
		<artifactId>uvms-pom</artifactId>
		<version>3.0</version>
	</parent>

	<groupId>eu.europa.ec.fisheries.uvms.movement</groupId>
	<artifactId>movement</artifactId>
	<version>5.5.1-UVMS-SNAPSHOT</version>
	<packaging>pom</packaging>
	<description>Maven project-archetype for UnionVms modules</description>

	<properties>
		<movement.api.check.against.version>4.0.0</movement.api.check.against.version>

		<uvms.pom.version>3.0</uvms.pom.version>
		<docker.dev.version>3.40.19.w18-JsonBOverJackson</docker.dev.version>
		<docker.liquibase.changeLogFile>LIQUIBASE/changelog/db-changelog-master-postgres.xml</docker.liquibase.changeLogFile>
        <docker.liquibase.db.user>movement</docker.liquibase.db.user>
        <docker.liquibase.db.passwd>movement</docker.liquibase.db.passwd>
		<longpolling.version>2.0.13</longpolling.version>
		<user.model.version>2.1.0</user.model.version>
		<audit.model.version>4.2.0</audit.model.version>
		<exchange.model.version>5.2.0-UVMS</exchange.model.version>
		<asset.client.version>6.6.0</asset.client.version>
		<movement-rules.model.version>2.3.0</movement-rules.model.version>
		<installAtEnd>false</installAtEnd>

		<!-- UVMS Libraries -->
		<uvms.common.version>4.1.1</uvms.common.version>
		<usm4uvms.version>4.1.0</usm4uvms.version>
		<uvms.config.version>4.1.0</uvms.config.version>

	</properties>

	<dependencies>
		<dependency>
			<groupId>jakarta.platform</groupId>
			<artifactId>jakarta.jakartaee-api</artifactId>
			<scope>provided</scope>
		</dependency>
	</dependencies>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>fish.focus.uvms.maven</groupId>
				<artifactId>uvms-pom-gis-deps</artifactId>
				<version>${uvms.pom.version}</version>
				<type>pom</type>
			</dependency>
			<dependency>
				<groupId>fish.focus.uvms.maven</groupId>
				<artifactId>uvms-pom-logging-deps</artifactId>
				<version>${uvms.pom.version}</version>
				<type>pom</type>
			</dependency>
			<dependency>
				<groupId>fish.focus.uvms.maven</groupId>
				<artifactId>uvms-pom-monitoring-deps</artifactId>
				<version>${uvms.pom.version}</version>
				<type>pom</type>
			</dependency>
			<dependency>
				<groupId>fish.focus.uvms.maven</groupId>
				<artifactId>uvms-pom-test-deps</artifactId>
				<version>${uvms.pom.version}</version>
				<type>pom</type>
			</dependency>
			<dependency>
				<groupId>fish.focus.uvms.maven</groupId>
				<artifactId>uvms-pom-arquillian-deps</artifactId>
				<version>${uvms.pom.version}</version>
				<type>pom</type>
			</dependency>
			<dependency>
				<groupId>fish.focus.uvms.maven</groupId>
				<artifactId>uvms-pom-java11-deps</artifactId>
				<version>${uvms.pom.version}</version>
				<type>pom</type>
			</dependency>
			<dependency>
				<groupId>com.google.guava</groupId>
				<artifactId>guava</artifactId>
				<version>28.1-jre</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<repositories>
		<repository>
			<id>focus-repo</id>
			<name>Focus nexus repository</name>
			<url>http://nexus.focus.fish/nexus/repository/public/</url>
		</repository>
		<repository>
			<id>OSGEO GeoTools repo</id>
			<url>http://download.osgeo.org/webdav/geotools</url>
		</repository>
		<repository>
			<id>Hibernate Spatial repo</id>
			<url>http://www.hibernatespatial.org/repository</url>
		</repository>
	</repositories>

	<distributionManagement>
		<repository>
			<id>focus-releases</id>
			<name>UnionVms Repository</name>
			<url>http://nexus.focus.fish/nexus/content/repositories/releases/</url>
		</repository>
		<snapshotRepository>
			<id>focus-snapshots</id>
			<name>UnionVms Snapshot Repository</name>
			<url>http://nexus.focus.fish/nexus/content/repositories/snapshots/</url>
		</snapshotRepository>
	</distributionManagement>

	<scm>
		<connection>scm:git:https://github.com/UnionVMS/UVMS-MovementModule-APP.git</connection>
		<tag>HEAD</tag>
	</scm>

	<profiles>
		<profile>
			<id>enforce-jdk-version</id>
			<activation>
				<!-- To disable profile, define property -Dfocus-pom.enforce.jdk.version.disabled=true -->
				<property>
					<name>!focus-pom.enforce.jdk.version.disabled</name>
				</property>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-enforcer-plugin</artifactId>
						<inherited>false</inherited>
						<executions>
							<execution>
								<id>enforce-jdk-version</id>
								<goals>
									<goal>enforce</goal>
								</goals>
								<configuration>
									<rules>
										<enforceBytecodeVersion>
											<maxJdkVersion>${focus-pom.java.version}</maxJdkVersion>
											<!-- NOTE: xstream do contain support for multiple jdk:s -->
											<excludes>
												<exclude>com.thoughtworks.xstream:xstream</exclude>
											</excludes>
										</enforceBytecodeVersion>
									</rules>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-plugin-plugin</artifactId>
				<version>3.6.0</version>
				<configuration>
					<goalPrefix>plugin</goalPrefix>
					<outputDirectory>target/dir</outputDirectory>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<modules>
		<module>model</module>
		<module>LIQUIBASE</module>
		<module>rest</module>
		<module>service</module>
		<module>module</module>
        <module>client</module>
    </modules>
</project>
